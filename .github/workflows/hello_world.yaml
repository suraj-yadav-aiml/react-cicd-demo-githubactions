name: Great User
on : workflow_dispatch

jobs:
    greet:
        runs-on: ubuntu-latest
        steps:
            - name: Greet User
              run : echo "Hello User"
            - name: Bye User
              run : echo "Bye Bye !!!"